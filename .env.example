# PEXEK static production handoff
# Build with: pnpm install --frozen-lockfile && pnpm check && pnpm build
# Vercel: Framework Vite; Output directory dist/public.
# No secrets or provider credentials belong in this source package.
